# Frontend environment (consumed at build time by scripts/prepare-env.mjs)

# Public API base URL (backend HTTP endpoint)
API_URL=http://localhost:4900

# Socket endpoints
GROUP_SOCKET_URL=http://localhost:9000
PRIVATE_SOCKET_URL=http://localhost:10100

# Stripe publishable (public) key
STRIPE_PUBLIC_KEY=pk_test_xxxxxxxxxxxxxxxxxxxxxxxxx

# (Optional) Override Angular dev server port if you run `npm start`
FRONTEND_PORT=4200
